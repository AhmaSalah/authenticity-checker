# Configuration for API Connection

# Set to 'true' to use the internal Mock API (for testing without backend)
# Set to 'false' to connect to the real .NET Backend
NEXT_PUBLIC_USE_MOCK=true

# The URL of the .NET Backend (Only used if NEXT_PUBLIC_USE_MOCK=false)
# Example: http://localhost:5000/api/detect
NEXT_PUBLIC_API_URL=http://localhost:5000/api/detect
